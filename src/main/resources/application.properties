spring.application.name=munsterland-backend
server.port=${PORT:8083}

# Database configuration (use environment variables in production)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://ep-polished-tree-ag6yjc39-pooler.c-2.eu-central-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_6KnZOLYM0vRm&sslmode=require&channelBinding=require}
spring.datasource.username=${DATABASE_USERNAME:neondb_owner}
spring.datasource.password=${DATABASE_PASSWORD:npg_6KnZOLYM0vRm}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA configuration
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:false}

# JWT configuration
app.jwt.secret=${JWT_SECRET:change-this-to-a-very-long-random-secret-32+chars}
app.jwt.ttl-seconds=${JWT_TTL:3600}